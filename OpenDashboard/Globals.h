#pragma once
#include <string>
#include <vector>	

extern std::string removeLetters(std::string text);
extern bool isStrDigit(std::string text);
extern std::vector<std::string> split(std::string stringToBeSplitted, std::string delimeter);
